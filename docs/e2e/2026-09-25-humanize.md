# 종단 실행 — python-humanize/humanize (계획 C)

- 날짜: 2026-09-25 · 대상: 공개 OSS `python-humanize/humanize` main `91648c4` (pytest · src 레이아웃 · hatch-vcs)
- 리더: 이 레포의 `SKILL.md` 를 따르는 세션. 에이전트는 번들 문서(`agents/*.md`)를 역할로 받은 서브에이전트.
- 원격에는 아무것도 보내지 않았다(push · PR 없음).
- 🔴 사람 게이트(재현 확인 · GATE 1 `expected_after` · GATE 2 · GATE L)는 리더가 대신 통과시켰다 — 사람의 판단을 잰 실행이 아니다.

## 결과

| 트랙 | 버그 | 결과 |
|---|---|---|
| 정식 | **실제 버그** — `ordinal(-1)` 이 `-1th`(floor mod 로 음수 접미사가 틀림, 1..1000 × 성별 2000 중 1080 오답). 기존 테스트에 음수 없음 | P0 → P1 → GATE 1 → P2 → P3 → `run` **PASS 1회**(루프 0/3). R-CONTROL 3축: 변이 전 사본 0 위반 · 변이 후 1080/1140/60 위반 · alive 초록. P5b 생략(`ui` 없음 — 라이브러리) |
| 가벼운 | **심은 회귀** — `naturalsize(-1)` 이 `-1 Bytes`(`abs_bytes` → `bytes_` 로 바꾼 커밋) | `light-verify`: 기준선 사본 재현 · 수정 후 사라짐 · 회귀 새 빨강 0 → 표지 없음 |

## 이 실행이 잡은 결함 (모두 이 레포에서 고침)

| # | 결함 | 영향 | 고침 |
|---|---|---|---|
| 1 | 호스트 pytest `addopts = --color=yes` 가 요약 줄 앞에 ANSI 를 붙임 | `ran_fully` 불일치 → 모든 회귀 측정 무효 | pytest 템플릿 `bp_side.sh` 에 `--color=no` |
| 2 | 사본에 git 이 **무시하는** 생성 파일(`src/humanize/_version.py`)이 없어 import 가 exit 1 | R-CAUSE 가 «망가져서» 빨개지면 R-CONTROL 이 공허하게 통과 — 픽스처로 **거짓 PASS 재현** | 게이트: 변이 전 사본의 R-CAUSE 를 원본과 대조, 다르면 ENV. 템플릿: 루트 `src/` 의 무시된 `.py` 를 사본에 채움 |
| 3 | 조사자 문서가 «수정 후 사본에 적용될 patch» 의 제약을 말하지 않음 | 수정 모양에 따라 변이 patch 가 적용 실패(ENV) | 조사자 문서 — 수정 모양과 무관한 patch · `@baseline` 권장 |
| 4 | 루브릭 예시 `grep -q "<문구>"` 가 `-` 로 시작하는 문구에서 옵션으로 읽힘 | R-SYMPTOM 측정 오류 | 예시를 `grep -q -x -F -e` 로 |
| 5 | 작업공간의 probe 스크립트는 동결 집합 밖 | 판정 중 바뀌어도 모름 | 조사자 문서 — probe 는 루브릭 인라인 |
| 6 | GREEN 구현자가 RED 실행 명령을 받지 못함 | 추측으로 자기 확인 | RED 작성자가 명령을 보고, SKILL 이 GREEN 배정에 넘김 |
| 7 | 라이브러리에 «routes» · «공유 파일» 기준이 안 맞음 | 에이전트가 임의 해석 | 조사자 · 가벼운 수정자 문서에 라이브러리 기준 |
| 8 | 가벼운 트랙에서 기존 스위트가 이미 빨간 경우 · 빈 `expected_after` | 지침 없음 | 가벼운 수정자 문서 |
| 9 | P6 가 «원격이 GitHub 이면 PR» — 원격이 남의 OSS 여도 | 사용자 승인 스톱이 막지만 포크 판단이 없음 | SKILL — 포크 여부는 사용자가 정함 |

## 남긴 공백

- `pr_body.md` 가 한국어 고정이다 — 영어권 OSS 에는 옮겨야 한다(SKILL 에 «옮기고 보인다» 규칙만 추가).
- 사람 게이트를 사람이 통과시킨 종단은 아직 없다.
- vitest · 화면(`ui`) 트랙 · P5b 스윕은 이 실행에서 돌지 않았다.
- `ordinal` 음수 버그는 업스트림에 알리지 않았다.
